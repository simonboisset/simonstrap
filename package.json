{
  "name": "root",
  "private": true,
  "scripts": {
    "i": "lerna bootstrap",
    "build": "lerna run build",
    "start": "lerna run start",
    "changed": "lerna changed",
    "clean": "lerna clean",
    "type-check": "lerna run type-check"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/webpack",
      "**/babel-loader",
      "**/jest",
      "**/react-scripts",
      "**/eslint"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
